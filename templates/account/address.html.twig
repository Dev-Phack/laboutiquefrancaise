{% extends 'base.html.twig' %}

{% block title %}Mes addresses - La Boutique Française{% endblock %}

{% block content %}
    <a href="{{ path('account_address_add') }}" class="btn btn-info float-right btn-sm">Ajouter une adresse</a>
    <h2>Mes adresses</h2>
    {% if app.user.adresses | length ==0 %}
        <p class="text-center mt-5">Vous n'avez pas encore ajouté d'adresses. Pour le faire, <a href="{{ path('account_address_add') }}">cliquez ici</a></p>
    {% else %}
        <div class="row mt-5">
        {% for address in app.user.adresses %}
            <div class="col-md-4">
                <div class="card" style="width: 18rem;">
                    <img src="" alt="" class="card-img-top">
                    <div class="card-body">
                        <h5 class="card-title">{{ address.name }}</h5>
                        <p class="card-text">
                            {{ address.adress }},<br>
                            {{ address.postal }} - {{ address.city }} - {{ address.country }}
                        </p>
                        <a href="{{ path('account_address_edit', {'id' : address.id}) }}">Modifier</a> | <a href="{{ path('account_address_delete', {'id' : address.id}) }}">Supprimer</a>
                    </div>
                </div>
            </div>
        {% endfor %}
        </div>
    {% endif %}
{% endblock %}